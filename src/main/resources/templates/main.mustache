<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <title>Main page</title>
</head>
<body>
<div>
    <form method="post">
        <label for="taskName">Task </label>
        <input id="taskName" type="text" name = "text" placeholder="Enter the task" required/>

        <label for="tagName">Tag </label>
        <input id="tagName" type="text" name = "tag" placeholder="Enter the task" required/>

        <label for="projName">Choose project: </label>
        {{#projects}}
            <input type="radio" id="projName" name = "projectValue" value="{{id}}"/>{{name}}
        {{/projects}}

        <button type="submit">Add</button>
    </form>
</div>

<div>
    <h3>List of tasks</h3>
    {{#tasks}}
        <div>
            <b>{{id}}</b>
            <span>{{text}}</span>
            <u>{{tag}}</u>
            <strong>{{projectName}}</strong>
        </div>
    {{/tasks}}
</div>
</body>
</html>